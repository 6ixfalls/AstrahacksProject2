{
  "name": "astrahacks-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc",
    "run": "cd build/ && node index.js",
    "test": "npm run compile && npm run run"
  },
  "author": "6ixfalls, JavaNameConvention",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^16.11.4",
    "tslint": "^6.1.3"
  },
  "dependencies": {
    "dompurify": "^2.3.3",
    "express": "^4.17.1",
    "marked": "^3.0.8"
  }
}
